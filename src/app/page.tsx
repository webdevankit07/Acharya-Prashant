import Container from '@/components/Container';

const Home = () => {
    return (
        <main className='min-h-screen'>
            <Container>a</Container>
        </main>
    );
};

export default Home;
